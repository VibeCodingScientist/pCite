# Required
ANTHROPIC_API_KEY=sk-ant-...
GEMINI_API_KEY=

# Optional — increases PubMed rate limit from 3 to 10 req/s
# NCBI_API_KEY=

# Optional — OpenAlex polite pool email
# OPENALEX_EMAIL=research@ncite.org

# Optional — Claude model for extraction (default: claude-sonnet-4-6)
# CLAUDE_MODEL=claude-sonnet-4-6

# Optional — Gemini model for edge classification (default: gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash

# Optional — max disease-focused papers from PubMed (default: 2000)
# MetaboLights studies are fetched exhaustively; this limits the PubMed supplement
# PUBMED_MAX_PER_QUERY=2000
