# Required
ANTHROPIC_API_KEY=sk-ant-...

# Optional — increases PubMed rate limit from 3 to 10 req/s
# NCBI_API_KEY=

# Optional — OpenAlex polite pool email
# OPENALEX_EMAIL=research@ncite.org

# Optional — Claude model for all calls (default: claude-sonnet-4-6)
# CLAUDE_MODEL=claude-sonnet-4-6

# Optional — max papers per PubMed query (default: 2000)
# Two queries run: repository-anchored + disease-focused, then union+dedup
# PUBMED_MAX_PER_QUERY=2000
